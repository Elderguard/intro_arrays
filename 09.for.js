const numbers = [100,200,300,400,500,600];

// First expression: It is executed once.
// Second expression: condition of the execution.
// Third expression: executed at the end of the block.

for (let index = 0; index < numbers.length; index++) {
    console.log(numbers[index]);
}